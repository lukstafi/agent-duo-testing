# Agent Learnings (Staging)

This file collects agent-discovered learnings for later curation into CLAUDE.md / AGENTS.md.

<!-- Entry: duo-parallel-alpha-codex | 2026-02-04 -->
### Peer sync status files

The  directory contains  files with  values. Capturing these in observation logs provides a reliable, low-effort record of phase transitions and cross-agent messages.

<!-- End entry -->
<!-- Entry: duo-parallel-alpha-codex | 2026-02-04 -->
### Peer sync status files

The `.peer-sync/` directory contains `<agent>.status` files with `state|timestamp|message` values. Capturing these in observation logs provides a reliable, low-effort record of phase transitions and cross-agent messages.

<!-- End entry -->
